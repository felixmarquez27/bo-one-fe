import type { ThemeOptions } from '@mui/material/styles';

/**
 * Opciones de tema personalizadas para BO ONE
 */
export interface BOOneThemeOptions extends ThemeOptions {
    // Aquí se pueden agregar opciones personalizadas adicionales
}
